# UI 構成 (たたき台)

本格的な UI を組み始める前に、**画面の一覧・遷移・各画面に置くもの**を決めるためのメモ。見た目の決まりごと (色・余白・部品) は `docs/design-system.md` にあるので、ここでは扱わない。

- 状態の表し方: ✅ 決定 / 🟡 案 (要相談) / ❓ 未定 / 🔨 `feat/home-world-ui` で作る / ⏸ 保留
- 迷ったら「未決事項」に足しておき、決まったら本文に反映して消す。

## 参考にしているもの

| 参考 | 取り入れたい点 |
|---|---|
| `docs/reference/UI_ref.png` (自作コンセプト) | 画面の流れ (ホーム → スキャン → 解析中 → 結果 → すみか変化 → ミッション/記録)、色と部品の雰囲気 |
| Pokémon Sleep のホーム | 3D の世界を画面いっぱいに出し、UI は四隅に浮かべる。主役のキャラが中央。**次にやること**を 1 つだけ大きく示すボタン |
| Pokémon Sleep のメインメニュー | 下部の「メニュー」から全画面のメニューを開き、機能をカードのグリッドで並べる。グラフを最上部に置いて「自分の記録」を一目で見せる。閉じるボタンは画面下の大きな帯 |

※ 他社アプリのスクリーンショットは権利の都合でリポジトリに入れない。取り入れたい点を文章で残す。

## 体験の軸 (README から)

1. **朝** デイリーミッションを選ぶ
2. **日中** 買い物のたびにレシートを撮る → CO₂e がスコアになる
3. **夜** ミッションを振り返る → 達成が氷とペンギンに反映される
4. **放置すると氷が溶ける** — ホームを開くたびに「今の状態」が一目でわかること

→ ホームは「見るだけで状態がわかる」「次にやることが 1 つだけ示される」の 2 点を最優先にする。

## 画面一覧

| # | 画面 | 役割 | 参考 | 状態 |
|---|---|---|---|---|
| 1 | ホーム (すみか) | 3D の世界。氷の量・CO₂ 削減・ペンギンの様子を見せる | コンセプト①、Sleep ホーム | 🔨 (`HomeScreen.uxml`) |
| 2 | レシートスキャン | カメラで撮影。枠に合わせる案内、最近のスキャン | コンセプト② | 🔨 (`ScanScreen.uxml`) |
| 3 | 解析中 | 読み取り → 分類 → CO₂ 計算の進み具合 | コンセプト③ | 🔨 (`AnalyzingScreen.uxml`) |
| 4 | 解析結果 | 品目ごとの CO₂e、合計、「すみかに反映」 | コンセプト④ | 🔨 (`ResultScreen.uxml`) |
| 5 | すみか変化 | Before / After、氷の増減、ペンギンのひとこと | コンセプト⑤ | ⏸ 別途検討 |
| 6 | ミッション | デイリー / ウィークリー、進み具合、報酬 | コンセプト⑥ | 🔨 (`MissionScreen.uxml`) |
| 7 | 記録 | CO₂ の推移グラフ、連続達成日数、スキャン履歴 | コンセプト⑥、Sleep メニュー上部 | 🔨 (`RecordScreen.uxml`) |
| 8 | 図鑑 (Pokedex) | 住み着いたペンギンの一覧 | README の Game Logic | ❓ コンセプト画像に無い。次回 |
| 9 | メニュー | 全機能への入口 | Sleep メインメニュー | 🔨 (`MenuScreen.uxml`) |
| 10 | 設定 | 通知、データ削除、プライバシー方針、ライセンス | — | ❓ |
| 11 | 初回起動 | コンセプト説明、カメラ権限の説明、最初のペンギン | — | ❓ |

## 画面遷移

🟡 案:

```
                 ┌──────────── タブバー / 下部ナビ ────────────┐
                 │                                              │
  初回起動 ──▶ ホーム ◀──────────────────────────────┐          │
                 │                                    │          │
                 ├─▶ スキャン ─▶ 解析中 ─▶ 解析結果 ─▶ (すみか変化) ┘
                 │      (撮り直し / 失敗 ◀─┘)
                 ├─▶ ミッション
                 ├─▶ 記録
                 └─▶ メニュー ─▶ 図鑑 / 設定 / …
```

- スキャン〜すみか変化は**一方通行のフロー**。途中は下部ナビを隠し、左上の × で中断する (コンセプト②③と同じ)。
- 最後は必ずホームに戻る。すみか変化の見せ方は別途検討で、今は「すみかに反映」でホームに戻るだけ。

## 下部ナビゲーション

✅ **A: 5 タブ** — すみか / 記録 / **スキャン (中央・大)** / ミッション / メニュー

- よく使う画面に 1 タップで行け、「毎日開く理由」であるミッションを常に見せられる
- タブバーは全画面で共有する 1 つだけ (`Assets/UI/App/TabBar.uxml`)。すみか・記録・ミッションで表示し、スキャン〜解析結果のフローとメニューでは隠す

### 代替案 B: 3 ボタン (Sleep 型)

ペンギン / メニュー / スキャン。世界を広く見せられて迷わない一方、記録・ミッションが 2 タップになる。A で使ってみて、3D の世界が狭いと感じたら再検討する。

## 各画面に置くもの

### 1. ホーム

- **背景**: 3D の世界を全画面 (UI の後ろ)。ペンギンが中央付近に来るカメラ配置
- **上部**: 氷の量 / CO₂ 削減のステータスカード 2 枚、右上に設定
- **中央**: ペンギンの吹き出し (状態に応じて一言。「氷が減ってきたよ…」など)
- **下部の上**: 🟡 Sleep の「次の目標」にあたる**行動ボタン 1 つ** (例: 「今日のミッションを選ぶ」「夜の振り返り」「レシートを撮る」を時間帯と状態で出し分け)
- **下部**: タブバー
- ❓ 通知バッジ (未達成のミッション、新しいペンギン)

### 2. レシートスキャン

- ✅ カメラ映像、枠のガイド、シャッター (仮実装あり)
- × で閉じる、フラッシュ、ヘルプ
- 最近のスキャン (サムネイル) — ⚠️ **レシート画像は端末に保存しない方針**なので、サムネイルは出せない。日付・店の種類・CO₂e のカードに置き換える案

### 3. 解析中

- 3 段階の進み具合 (文字を読み取り中 → 商品を分類中 → CO₂ を計算中)
- ペンギンの待機アニメーション、「少しだけ待ってね…」
- 失敗時: 「読み取れなかった」→ 撮り直す / やめる

### 4. 解析結果

- 日時、店の種類
- 品目の一覧 (アイコン・カテゴリ・CO₂e)
- 合計 CO₂e を大きく、身近な例え (杉の木◯本分)
- 「すみかに反映」(強調ボタン、1 画面 1 つ)
- ❓ 品目を修正できるようにするか (AI の読み間違い対策)

### 5. すみか変化

- ⏸ 別途検討。案: (a) ホームに戻って 3D で氷が増える演出 + 結果カードを重ねる / (b) コンセプト⑤の Before/After を並べる専用画面。(b) は静止画になるので 3D の世界と二重になる点に注意

### 6. ミッション

- デイリー / ウィークリーの切り替え
- ミッションカード (アイコン、名前、進み具合、報酬 pt)
- 🟡 朝に「今日のミッションを選ぶ」、夜に「振り返る」— 選ぶ・振り返る操作をどこに置くか

### 7. 記録

- CO₂ の推移 (週の棒グラフ。Sleep のように最上部に大きく)
- 連続達成日数、エコポイント
- スキャン履歴 (明細は端末内のみ)

### 9. メニュー

- 🟡 Sleep 型: 全画面、機能をカード 2 列で並べる。閉じるボタンは下部の大きな帯
- 中身: 図鑑、記録、ミッション、設定、お知らせ、ヘルプ …

## 画面をまたぐ共通要素

- **状態**: 読み込み中 / 空 (まだ記録がない) / エラー (通信失敗・カメラ権限なし) を画面ごとに用意する
- **トースト / ダイアログ**: 保存・失敗の通知、確認
- **ペンギンの吹き出し**: ホーム以外でも案内役として使う (解析中、結果)
- **SafeArea**: ✅ `SafeAreaApplier` で対応済み

## 未決事項

- [ ] すみか変化の見せ方 (ホームでの演出 / 独立画面)
- [ ] 図鑑をどこから開くか (タブ / メニュー / ホームのペンギンをタップ)
- [ ] 朝のミッション選択・夜の振り返りの導線
- [ ] 解析結果の品目を手で直せるようにするか
- [ ] 最近のスキャンに何を表示するか (画像は保存しない)
- [ ] オフライン時にスキャンした場合の扱い
- [ ] 通知 (プッシュ) を使うか。使うなら何を知らせるか

## メモ

(自由に追記)
